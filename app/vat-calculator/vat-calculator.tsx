"use client";
import { useState } from "react";
const formatNumber = (num: number) => new Intl.NumberFormat("en-US", { maximumFractionDigits: 2 }).format(num);
export default function VatCalculator() {
  const [amount, setAmount] = useState<string>(""); const [taxRate, setTaxRate] = useState<string>(""); const [mode, setMode] = useState<"exclusive" | "inclusive">("exclusive");
  const baseAmount = parseFloat(amount); const rate = parseFloat(taxRate); const isValid = !isNaN(baseAmount) && baseAmount > 0 && !isNaN(rate) && rate >= 0;
  const taxAmount = isValid && mode === "exclusive" ? (baseAmount * rate) / 100 : isValid && mode === "inclusive" ? baseAmount - baseAmount / (1 + rate / 100) : null;
  const finalPrice = isValid && mode === "exclusive" ? baseAmount + (baseAmount * rate) / 100 : isValid && mode === "inclusive" ? baseAmount : null;
  const netPrice = isValid && mode === "inclusive" ? baseAmount / (1 + rate / 100) : null;
  const loadExample = () => { setAmount("100"); setTaxRate("15"); setMode("exclusive"); }; const reset = () => { setAmount(""); setTaxRate(""); setMode("exclusive"); };
  return <div className="py-10 px-4"><div className="max-w-xl mx-auto bg-white rounded-3xl shadow-xl border border-blue-100 p-8"><h1 className="text-3xl font-bold text-gray-900 text-center mb-2">VAT / Sales Tax Calculator</h1><p className="text-center text-gray-500 mb-8">Calculate tax amount, net price, and final price.</p><div className="space-y-6"><div><label htmlFor="amount" className="block text-sm font-semibold text-gray-700 mb-1">Amount ($)</label><input id="amount" type="number" inputMode="decimal" min="0" step="0.01" value={amount} onChange={(e) => setAmount(e.target.value)} placeholder="e.g. 100" className="w-full px-4 py-3 border border-blue-200 rounded-xl text-lg text-gray-900 focus:outline-none focus:ring-2 focus:ring-blue-500 bg-blue-50/30" /></div><div><label htmlFor="taxRate" className="block text-sm font-semibold text-gray-700 mb-1">Tax Rate (%)</label><input id="taxRate" type="number" inputMode="decimal" min="0" step="0.01" value={taxRate} onChange={(e) => setTaxRate(e.target.value)} placeholder="e.g. 15" className="w-full px-4 py-3 border border-blue-200 rounded-xl text-lg text-gray-900 focus:outline-none focus:ring-2 focus:ring-blue-500 bg-blue-50/30" /></div><div><span className="block text-sm font-semibold text-gray-700 mb-1">Calculation Mode</span><div className="flex gap-2"><button onClick={() => setMode("exclusive")} className={`flex-1 px-4 py-3 rounded-xl border font-semibold transition ${mode === "exclusive" ? "bg-blue-600 text-white border-blue-600" : "bg-white text-gray-700 border-gray-300 hover:bg-blue-50"}`}>Tax Exclusive</button><button onClick={() => setMode("inclusive")} className={`flex-1 px-4 py-3 rounded-xl border font-semibold transition ${mode === "inclusive" ? "bg-blue-600 text-white border-blue-600" : "bg-white text-gray-700 border-gray-300 hover:bg-blue-50"}`}>Tax Inclusive</button></div></div><div className="flex gap-3 pt-2"><button onClick={loadExample} className="flex-1 px-4 py-3 bg-blue-600 text-white font-semibold rounded-xl hover:bg-blue-700 transition shadow-md shadow-blue-200">Load Example</button><button onClick={reset} className="flex-1 px-4 py-3 bg-white text-blue-700 font-semibold rounded-xl border border-blue-300 hover:bg-blue-50 transition">Reset</button></div></div>{isValid && <div className="mt-8 bg-gradient-to-r from-blue-50 to-cyan-50 p-6 rounded-2xl border border-blue-100"><h2 className="text-xl font-bold text-gray-800 mb-4">Results</h2><div className="space-y-4">{mode === "exclusive" ? <><div className="flex items-center justify-between"><span className="text-gray-600 font-medium">Net Price (Before Tax)</span><span className="text-2xl font-bold text-blue-600">${formatNumber(baseAmount)}</span></div><div className="flex items-center justify-between"><span className="text-gray-600 font-medium">Tax Amount</span><span className="text-2xl font-bold text-teal-600">${formatNumber(taxAmount!)}</span></div><div className="flex items-center justify-between"><span className="text-gray-600 font-medium">Final Price (After Tax)</span><span className="text-2xl font-bold text-green-600">${formatNumber(finalPrice!)}</span></div></> : <><div className="flex items-center justify-between"><span className="text-gray-600 font-medium">Gross Price (Including Tax)</span><span className="text-2xl font-bold text-blue-600">${formatNumber(baseAmount)}</span></div><div className="flex items-center justify-between"><span className="text-gray-600 font-medium">Tax Amount</span><span className="text-2xl font-bold text-teal-600">${formatNumber(taxAmount!)}</span></div><div className="flex items-center justify-between"><span className="text-gray-600 font-medium">Net Price (Before Tax)</span><span className="text-2xl font-bold text-green-600">${formatNumber(netPrice!)}</span></div></>}</div></div>}</div></div>;
}
